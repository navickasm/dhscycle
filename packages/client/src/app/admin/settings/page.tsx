'use client';

//sitewide settings (school year dates, passing periods, etc)
export default function AdminSettingsPage() {
    return (
        <div>
            <h1>Settings</h1>
        </div>
    );
}
